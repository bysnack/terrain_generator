#include "TerrainGenerator.hpp"
#include <iostream>

int main(int argc, char** argv) {
    // initialize terrain forwarding command line arguments
    // used by both the class and gult
    TerrainGenerator::init(argc, argv);

    return 0;
}